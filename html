<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Valentine's Day for Akshu</title>
    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to bottom right, #f472b6, #ef4444); /* Pink to Red */
            color: white;
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* Floating Hearts Animation */
        .floating-hearts {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        .heart {
            position: absolute;
            font-size: 2rem;
            animation: float 10s infinite linear;
        }

        .heart:nth-child(odd) {
            animation-duration: 12s;
        }

        .heart:nth-child(even) {
            animation-duration: 8s;
        }

        @keyframes float {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* Memory Card Hover Pulse */
        .memory-card:hover {
            animation: pulse 1s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
        }

        /* Button Styles */
        .btn {
            font-size: 1.25rem;
            padding: 1rem 2rem;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .btn-yes {
            background: #22c55e;
            animation: bounce 2s infinite;
        }

        .btn-yes:hover {
            background: #16a34a;
        }

        .btn-no {
            background: #ef4444;
            position: relative;
        }

        .btn-no:hover {
            background: #dc2626;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        /* Sections */
        section {
            padding: 4rem 1rem;
            text-align: center;
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            animation: pulse 2s infinite;
        }

        .hero .cupid {
            animation: bounce 2s infinite;
        }

        .memories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .memory-card {
            background: rgba(255, 255, 255, 0.2);
            padding: 1.5rem;
            border-radius: 10px;
            transition: transform 0.3s;
        }

        .memory-card h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .memory-card .emoji {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .question {
            position: relative;
        }

        .buttons {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }

        .success-message {
            display: none;
            font-size: 1.5rem;
            font-weight: bold;
            animation: pulse 2s infinite;
            margin-top: 2rem;
        }

        .music-player {
            position: fixed;
            bottom: 1rem;
            right: 1rem;
            background: rgba(255, 255, 255, 0.2);
            padding: 1rem;
            border-radius: 10px;
            font-size: 0.875rem;
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2rem;
            }
            .memories-grid {
                grid-template-columns: 1fr;
            }
            .buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <!-- Floating Hearts Background -->
    <div class="floating-hearts">
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
        <span class="heart">❤️</span>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Hey Akshu <span class="cupid">🏹</span></h1>
        <p>Will you be my Valentine? 💕</p>
    </section>

    <!-- Love Message Section -->
    <section>
        <h2>Our Special Memories 🌹</h2>
        <p>Remember doing potato together? You love small cute things, we love to kiss 😘, you adore pasta 🍝, and my goatee always makes you smile! Let's make more memories. ❤️</p>
    </section>

    <!-- Memories Grid -->
    <section>
        <div class="memories-grid">
            <div class="memory-card">
                <div class="emoji">🥔</div>
                <h3>Doing Potato</h3>
                <p>Our fun adventures with potatoes!</p>
            </div>
            <div class="memory-card">
                <div class="emoji">🧸</div>
                <h3>Small Cute Things</h3>
                <p>You love all the tiny treasures!</p>
            </div>
            <div class="memory-card">
                <div class="emoji">😘</div>
                <h3>Kisses</h3>
                <p>Our sweetest moments!</p>
            </div>
            <div class="memory-card">
                <div class="emoji">🍝</div>
                <h3>Pasta</h3>
                <p>Your favorite dish, made with love!</p>
            </div>
        </div>
    </section>

    <!-- Question Section -->
    <section class="question">
        <h2>So, Akshu... 💖</h2>
        <div class="buttons">
            <button class="btn btn-yes" id="yes-btn">Yes! 😍</button>
            <button class="btn btn-no" id="no-btn">No 😢</button>
        </div>
        <div class="success-message" id="success-msg">Yay! You said yes! Let's celebrate! 🎉❤️</div>
    </section>

    <!-- Music Player -->
    <div class="music-player">
        <p>🎵 Die with a Smile - Bruno Mars & Lady Gaga</p>
        <button id="play-pause-btn">Play</button>
        <audio id="audio-player" src="path/to/die-with-a-smile.mp3" preload="none"></audio> <!-- Replace with actual path -->
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <script>
        // Music Player
        const audio = document.getElementById('audio-player');
        const playPauseBtn = document.getElementById('play-pause-btn');
        playPauseBtn.addEventListener('click', () => {
            if (audio.paused) {
                audio.play();
                playPauseBtn.textContent = 'Pause';
            } else {
                audio.pause();
                playPauseBtn.textContent = 'Play';
            }
        });

        // No Button Run Away
        const noBtn = document.getElementById('no-btn');
        noBtn.addEventListener('mouseenter', () => {
            const container = noBtn.parentElement;
            const containerRect = container.getBoundingClientRect();
            const btnRect = noBtn.getBoundingClientRect();
            const maxX = containerRect.width - btnRect.width;
            const maxY = containerRect.height - btnRect.height;
            const newX = Math.random() * maxX;
            const newY = Math.random() * maxY;
            noBtn.style.position = 'absolute';
            noBtn.style.left = `${newX}px`;
            noBtn.style.top = `${newY}px`;
        });

        // Yes Button Confetti
        const yesBtn = document.getElementById('yes-btn');
        const successMsg = document.getElementById('success-msg');
        yesBtn.addEventListener('click', () => {
            confetti({
                particleCount: 100,
                spread: 70,
                origin: { y: 0.6 }
            });
            successMsg.style.display = 'block';
            setTimeout(() => {
                successMsg.style.display = 'none';
            }, 5000); // Hide after 5 seconds
        });
    </script>
</body>
</html>
